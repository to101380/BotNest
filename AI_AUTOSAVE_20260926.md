# AI 助理自動儲存

- 身分與語氣、回覆規則：文字停止輸入約 700 ms 後儲存，開關與選單變更立即儲存。移除「儲存設定」按鈕，顯示等待、儲存中、成功或失敗狀態。
- 知識編輯：標題與內容有效後自動新增／更新；「完成」及關閉編輯器會等候儲存。新增、編輯、啟用切換、刪除與匯入均持久化，不必另按設定儲存。匯入仍先存草稿，由使用者核對後啟用。
- 寫入依序執行，伺服器舊回應不覆蓋正在輸入的新文字。中文組字期間暫停提交；同帳號切換站內頁面會提交待存內容。未存完關閉分頁時會提示；帳號切換會取消舊編輯器與請求，避免跨帳號寫入。
- 儲存失敗保留目前輸入並提供重試；網路恢復時重試。必填內容不完整時明確顯示尚未儲存。知識編輯可明確放棄未儲存部分，已儲存資料保留。
- 新知識使用帳號範圍內的固定草稿 UUID，重試建立不重複計費額度、不覆寫既有內容；後續 PUT 儲存最新輸入。資料庫交易拒絕更新已刪除資料，避免延遲寫入使其復活。

## 驗證

- 完整本機 163 項測試通過，44 個 JavaScript 檔語法檢查通過。
- 正式快照隔離部署包 60 項後端測試通過。
- 使用假資料的 Edge 瀏覽器驗證：設定自動儲存、請求未結束時繼續編輯、失敗重試、站內切頁、回覆規則開關、知識新增／修改／啟用／刪除，以及重新整理後資料保留。一次新增僅建立一筆資料；無瀏覽器程式錯誤。
- 桌面與 390 px 手機版已檢視，沒有水平溢出。未修改真實商家設定或顧客資料，未發送顧客訊息。

## 部署方式

以正式 Hosting `28243eba612fab10` 和當時正式 `botnestApi` 為底，僅更新 3 個前端檔案與 2 個後端模組。工作區其他未完成變更未混入；資料庫規則及兩個 AI 回覆觸發器不在本次部署範圍。


## 正式發布結果

- 2026-09-26 UTC 13:35:46 更新 `botnestApi`，狀態 ACTIVE；下載正式原始碼後，所有檔案 SHA-256 均與測試過的隔離包一致。
- Hosting 已發布 `a465fc6a00a5955d`，正式網站全部 30 個資源 SHA-256 與部署包一致。健康檢查 HTTP 200，未登入設定 API HTTP 401。
- 重新整理網站後即可使用。實際商家帳號的儲存驗收留給使用者操作，本次瀏覽器寫入測試只使用本機假資料。
