# 對話列表篩選與姓名搜尋

- 對話列表新增「全部／AI 回覆中／真人接手／關閉 AI」，可與用戶姓名搜尋同時使用。
- 比對平台姓名與顧客自訂姓名，支援部分字元及依序字元、忽略大小寫、空白與全半形差異；例如「黃博」可找到「黃韋博」。
- 搜尋及篩選會繼續讀取後續對話頁，顯示已搜尋筆數；失敗或游標不前進時停止並保留「尚有更多對話」，不宣稱已搜尋完畢。清除條件停止後續掃描。
- 列表 API 僅對已驗證屬於目前使用者與渠道的對話補充 AI 狀態。每頁讀一次商家設定及各對話控制狀態；不向每位用戶個別發出前端 HTTP 請求。
- 真人接手包含真人回覆後的暫停；全域／渠道停用、對話關閉及目前不在自動回覆時段歸為關閉 AI，列上的提示文字保留原因。缺少狀態時顯示待確認，不猜測為自動回覆。
- 切換接手模式立即更新目前列表；其他狀態沿用現有輪詢，按重新整理會重新載入搜尋結果。

## 驗證

完整工作區 166 項測試通過，46 個 JavaScript 檔語法檢查通過。Edge 假資料驗證跨頁姓名搜尋、狀態與姓名同時篩選、全形英文、切換接手模式即時更新；桌面與 390 px 手機畫面已檢視。

隔離正式快照 92 項測試中 90 項通過，2 項 OAuth 測試失敗。將同樣測試套用未修改的正式原始碼，重現完全相同的 2 項失敗，屬本機尚未部署的 OAuth 邏輯與正式版差異；本次未修改 OAuth。

以正式 Hosting a465fc6a00a5955d 與當時的 botnestApi 為底，只部署 4 個前端檔、後端 core.js 的 3 處接線與新 inbox-ai.js；不混入工作區其他未完成變更，不修改商家設定或傳送顧客訊息。

## 正式部署

- botnestApi 更新於 2026-09-26 UTC 14:00:21，ACTIVE；正式原始碼全部 SHA-256 與隔離部署包一致。
- Hosting 發布 3d37b83c8a067ece，32 個正式資源全部 SHA-256 相符；健康檢查 HTTP 200、未登入設定 API HTTP 401。
