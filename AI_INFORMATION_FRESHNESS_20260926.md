# AI 回覆舊資訊：檢查與修補

使用者回報更新商家資訊、客服指示詞及知識庫後，LINE、Instagram、Messenger 仍回答舊資訊。

## 已重現的程式問題

1. 知識片段超過 8 個時，所有資料一起按詞彙分數排序，最新的商家資訊也可能被 8 個舊片段擠掉。測試以「目前 990 元」及 8 筆「舊版 499 元」重現最新資訊未送入模型。
2. 搜尋知識的問題文字包含先前 AI 的回答，造成舊回答的詞彙把舊文件推到前面。
3. 自動回覆在生成後會重新檢查設定，卻未檢查知識庫；因此生成途中更新／停用／刪除知識，仍可能送出先前生成的答案。

## 修補行為

- 每次保留完整、大小已受輸入驗證限制的目前商家資訊與補充指示，其餘最多 7 個知識片段依相關性選取。沒有商家資訊時仍可選 8 個片段。
- 僅顧客最近的問題參與知識排序，先前 AI 回答只保留為對話背景；明確要求目前資料優先於舊對話，資料彼此矛盾時轉真人。
- 對實際啟用的知識內容建立 SHA-256 版本指紋，回覆生成完成及社群分段傳送前重新比對。知識有變動則取消尚未送出的內容，下一則新訊息使用新資料。
- 待續傳的已生成答案必須有相同設定／知識版本；無版本的舊紀錄不直接續送。已送出的訊息不撤回，不自動重新向顧客發送。
- 商家資訊最長 6,000 字、補充指示最長 4,000 字，完整保留後模型輸入可能比原先略多；知識版本核對會增加唯讀查詢。

## 驗證與實際限制

- 修改前，新增回歸測試確實重現「新商家資訊被擠掉」和「舊 AI 答案影響排名」兩項失敗；修補後通過。
- 156 項本機測試通過，涵蓋三渠道生成途中變更商家設定／更新、停用、刪除知識，以及 IG／Messenger 分段間變更知識。不呼叫真實付費模型或向顧客傳送訊息。
- 重新登入後，唯讀診斷確認三渠道 AI 啟用，帳戶有 3 筆已啟用的知識。下載正式 Functions 原始碼後確認包含上述程式缺陷；未逐條裁定商家文件間的內容矛盾，不能斷言這些缺陷是個別舊回答的唯一原因。
- 匯入資料仍預設草稿，必須啟用；程式不會擅自停用或覆寫其他知識。
- 此修改只更新 BotNest-upload 正式來源，未改動 google-meta-login 歷史副本。

## 正式部署與驗證

- 2026-09-26 台北時間 21:06 完成 `planning-with-ai-52d58` 的 `botnestApi`、`lineAiAutoReply`、`facebookAiAutoReply` 更新（後者同時處理 Instagram）。三函式更新時間分別為 UTC 13:06:23、13:06:36、13:06:28，部署後均為 ACTIVE。
- 以下載的正式版為底建立隔離部署包，只替換本次已提交的 `ai-policy.js`、`ai-engine.js`、`ai-responder.js`，避免帶入工作區其他未完成變更。未發布 Hosting 或修改資料庫規則。
- 隔離包 59 項 AI 管理與資訊更新回歸測試全部通過，JavaScript 語法檢查通過。部署後再次下載三函式原始碼，所有正式原始檔均與隔離包 SHA-256 一致。
- 正式站 `/api/line/health` 回傳 HTTP 200；未登入存取 `/api/ai/settings` 回傳 HTTP 401。
- 沒有向顧客傳送測試訊息，也沒有呼叫付費模型。仍需本人從 LINE、IG、Messenger 各傳一則新問題，核對答案是否符合已儲存的商家資訊與啟用知識；既有聊天紀錄不會被改寫。
