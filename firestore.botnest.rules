rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All application data is server-owned. Admin SDK access requires IAM;
    // the HTTPS API separately verifies identity, device approval and ownership.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
