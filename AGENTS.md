# 專案協作規範

## 每次更新都同步 GitHub

使用者已明確要求：以後每次更新完成，都要提交並推送到本專案的 GitHub 倉庫。

- 倉庫：`https://github.com/to101380/BotNest.git`（`origin`）。
- 每次完成程式或文件更新，執行適當驗證後，建立清楚描述變更的 commit，並推送到對應的遠端分支；一般更新不必再次詢問是否上傳。
- 只提交本次工作範圍內的變更。工作目錄可能存在其他尚未完成的修改，應保留並避免混入；不要直接使用 `git add .`。
- 不上傳憑證、私鑰、環境秘密、顧客資料或暫存部署目錄。
- 保留遠端既有歷史，不強制推送。若推送因權限、認證或衝突受阻，清楚回報尚未同步的內容與原因。
- 完成回報應包含 GitHub 同步結果及 commit 或 PR 連結；網站部署與 GitHub 推送要分別確認。

## UI 方向

後續 UI 更新遵循 [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md)：簡潔介面、滑動選取底框、平順的內容切換、半透明與毛玻璃表面。
